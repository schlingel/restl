package at.fundev.restl.processor;

import java.io.InputStream;

/**
 * <p>The producer interface is used in the {@see RestlHttpService} to </p>
 */
public interface Producer {
	public Result produce(InputStream input);
}
